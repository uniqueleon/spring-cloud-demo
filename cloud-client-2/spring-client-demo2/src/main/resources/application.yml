server:
    port: 8898
    
spring:
  application:
    name: apptest2
  profiles:
    active: dev
  redis:
    host: 127.0.0.1
    port: 6379
    jedis:
      pool:
        max-active: 8
      
  
feign:
  okhttp:
    enabled: true
  client:
    config:
      feignName:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full
        decode404: false 
  hystrix:
    enabled: true